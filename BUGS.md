# Известные проблемы — вкладка Monitor (ControlMyMonitor 1.43)

Записано 2026-08-23. Функционал есть и работает, но есть огрехи — чинить, когда будет время.

## Статус волны 1 (2026-09-01, поздняя сессия)
- ✅ #1 Game двойной тап: лишний Resize убран (⤡ только по выбору), prompt() заменён модалками, у всех элементов ✎/✕
- ✅ #2 edge-чекбокс: персист + визуал зоны при off починены
- ✅ #3 edge-зоны в пикселях (Game); Mouse/Stream пока в %, как были
- 🔄 #4 монитор: юзер ПЕРЕДУМАЛ — добавлен низкоуровневый dxva2-блок ПАРАЛЛЕЛЬНО старому (/api/monitor2/*), старый не тронут; после теста один из двух выпиливается

## Баги, найденные после коммита 3 (2026-09-01) — вход в спеку переделки UI

1. **Game: двойной тап** — панель предлагает лишний «Resize» (ресайз и так есть через
   хэндл в углу: тап-выбор элемента → драг значка). Кнопка 🔑 открывает нативный
   `prompt()` — убрать, заменить на нормальный UI. Для кастомных элементов до сих пор
   нет удобного удаления/изменения (есть только хэндл ✕, который не очевиден).
2. **Settings: чекбокс edge zone** — визуально переключается, пока сидишь на вкладке,
   но после выхода возвращается в «вкл»; по факту элементы тоже не меняются.
   Баг применения/персистентности `settings.edgeEnabled` — разобрать при реализации.
3. **Edge-зоны: размер в пикселях, а не в процентах** — чтобы на неквадратных тачпадах
   зоны выглядели ровно (сейчас % от стороны искажает квадратные зоны на прямоугольнике).
4. **Монитор** — ControlMyMonitor остаётся (решение пользователя, 2026-09-01):
   стало лучше после кэша/ретраев, дальше не ковыряем. Прямой DDC/CI (dxva2) — не делать.

## Спек-пожелания к большой переделке UI (обсудить в новой сессии)

- Полное редактирование структуры страниц: создание/удаление/полное редактирование
  разделов (не «гибрид», а единый декларативный движок вёрстки).
- Stream: все нюансы (кнопки, тачпады, вьюпорт) — через то же редактирование;
  учесть **и портретную, и альбомную** ориентацию.
- Game: масштабирование элементов относительно вьюпорта **ИЛИ** профили раскладки
  per-устройство (определение по IP/UA) — чтобы на двух разных экранах элементы
  были там, где настроены. Базовая схема: позиции в % вьюпорта, размеры в px.
- Game: двойной тап = «удалить/изменить» (без дублирующего resize, без prompt()).

**Статус после коммита 2 (2026-09-01):**
- ✅ «FPS не работает» — починено (sw.Restart() в цикле стрима)
- ✅ «Прокручивает прошедшие кадры» — latest-frame: старые кадры отбрасываются
- ✅ Сессия рвётся при скрытии вкладки браузера (visibilitychange)
- 🔧 #1/#2/#3 (монитор) — применено лечение: кэш состояния (TTL), ретраи чтений,
  пауза чтений 2с после power-операций, без чтения значения обратно после записи
- ⏳ Прямой DDC/CI (dxva2.dll) для яркости — **решено не делать** (см. выше)

## 1. При открытии вкладки значения не отображаются
- Яркость/питание при открытии вкладки показывают `--`, хотя сервер значения, возможно, отдаёт
- После того как пользователь сам что-то поменял, значение отображается — но это то, что ввёл сам клиент, а не подтверждённое сервером
- Аналогичное поведение уже было со слайдером громкости
- **Дальше:** глянуть сырой JSON `GET /api/monitor/state` (devtools/curl): что реально приходит (null? число? задержка запроса?)

## 2. Power on/off показывается нестабильно
- Иногда верно (on=on, off=off), но чаще «от балды»
- Вероятно: DDC/CI-чтения flaky + читаем состояние сразу после переключения, пока монитор в переходе
- **Дальше:** ретраи чтения (2–3 попытки с паузой) в `MonitorService.GetValue`; задержка перед чтением после TurnOn/TurnOff
- ControlMyMonitor 1.43 имеет GUI-опцию «Try to read VCP code again if it fails in the first time» — подтверждает, что чтения падают

## 3. После выкл→вкл монитора яркость на клиенте сбрасывается на 0
- Сам монитор де-факто держит ту же яркость, а клиент после wake показывает 0
- Похоже, DDC-чтение сразу после wake возвращает 0/мусор
- **Дальше:** задержка + ретраи перед первым чтением после TurnOn; на клиенте не сбрасывать последнее известное значение

## Контекст
- ControlMyMonitor **v1.43** (NirSoft); команды: `/GetValue` (значение в exit code), `/SetValue`, `/TurnOn`, `/TurnOff`, `/SwitchOffOn`, `/stab`
- VCP: `10` = Brightness (0–100), `D6` = Power Mode (1=on, 4=standby, 5=suspend; у монитора possible values: 1,4,5)
- Код: `RemoteControl.server/Services/MonitorService.cs`, эндпоинты `/api/monitor/state|brightness|power`, UI — вкладка Monitor в `wwwroot/index.html`
